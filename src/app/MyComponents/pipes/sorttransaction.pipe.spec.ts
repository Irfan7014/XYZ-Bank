import { SorttransactionPipe } from './sorttransaction.pipe';

describe('SorttransactionPipe', () => {
  it('create an instance', () => {
    const pipe = new SorttransactionPipe();
    expect(pipe).toBeTruthy();
  });
});
